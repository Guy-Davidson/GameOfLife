import { DarkMain } from "./DarkMain"

export const DarkTheme = {
    id: 'dark', 
    App: {
        fontColor: {
            main: DarkMain.fontGrey
        },
        backgroundColor: {
            main: DarkMain.mainBgBlack,
        }
    }
}